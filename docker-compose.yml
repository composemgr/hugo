# 
name: hugo
services:
    server:
        image: 'peaceiris/hugo:latest'
        restart: always 
        pull_policy: always
        command: 'hugo server --bind 0.0.0.0 --port 80 -s /site'
        tty: true
        stdin_open: true
        ports:
            - '172.17.0.1:60190:80'
        volumes:
            - '$(pwd):/site'
        networks:
          - hugo

networks:
  hugo:
    name: hugo
    external: false
    
